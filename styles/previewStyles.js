import css from 'next/css';

const styles = {
  flex: 1,
  width: 650,
  maxWidth: '100%',
  margin: 'auto',
  padding: 20,
  boxSizing: 'border-box',
  fontSize: 20,
  color: 'rgba(0,0,0,0.95)',
};

export default css(styles);
